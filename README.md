# 🃏 Rust DCFR Poker Solver

Легковесный, но мощный солвер для Техасского Холдема (Heads-Up No-Limit), написанный на Rust. Использует алгоритм **DCFR** (Discounted Counterfactual Regret Minimization) и быстрые таблицы оценки рук (Cactus Kev).

## 🚀 Быстрый старт

**Важно:** Всегда запускайте в режиме `release`, иначе вычисления будут идти в 50 раз медленнее.

```bash
# Сборка и запуск
cargo run --release

# Опции внутри:
# [1] Play  - Играть против бота
# [2] Train - Обучить (рекомендуется > 10,000 итераций)
```

## 🧠 Схема работы (Архитектура)

Солвер работает в три этапа. Вот упрощенная схема потока данных:

```mermaid
graph TD
    A[Start Iteration] --> B{Shuffle Deck};
    B --> C[Precompute Hand Buckets];
    C -- "O(1) Lookup" --> D[Tree Traversal (CFR)];
    
    subgraph "Recursive CFR Walk"
    D --> E{Is Terminal?};
    E -- Yes --> F[Return Payoff];
    E -- No --> G[Get InfoSet Key];
    G --> H[Update Regrets & Strategy];
    end
    
    H --> I[Save 'Brain' to Disk];
```

### Ключевые компоненты:
1.  **Hand Evaluator:** Табличный метод (Lookup Tables). Оценка 7 карт за наносекунды без перебора.
2.  **Abstractions (Абстракции):**
    *   *Preflop:* 169 канонических рук.
    *   *Postflop:* Группировка рук в 20 "корзин" (buckets) по силе (Equity).
3.  **Optimization:** Эквити рук для каждой улицы считается **один раз** перед обходом дерева, что дает скорость >5000 итераций/сек.

## 🛠 Технические особенности
*   **Алгоритм:** DCFR (быстрее сходится, чем ванильный CFR).
*   **Ставки:** 7 размеров (33%, 50%, 75%, 100%, 150%, All-in).
*   **Ограничения:** Максимум 3 рейза на улицу (для сокращения ветвления).
*   **River Resolving:** На ривере используется точный перебор всех исходов вместо Монте-Карло.

## 🔮 Предложения по улучшению (Roadmap)

Текущая версия играет сильно, но её можно превратить в "убийцу" профессионалов:

### 1. Внедрение Нейросети (Deep Learning) 🌟
Вместо хранения гигантского дерева в памяти, использовать нейросеть для оценки ситуаций на лету (подход DeepStack/Libratus).
*   **Как:** Обучить MLP (PyTorch) предсказывать EV на основе борда и диапазонов.
*   **Зачем:** Позволит считать бесконечно глубокие стратегии без абстракций.

### 2. Улучшение Абстракций
Сейчас бот смотрит только на "Шанс победы" (Equity).
*   **Проблема:** Бот путает "готовые слабые руки" и "сильные дро".
*   **Решение:** Внедрить метрику **OCH** (Oppo-Cluster Hand strength) или **EMD**, учитывающую потенциал руки (Potential Awareness).

### 3. Блокеры (Card Removal)
Добавить учет того, что карты на руках у бота блокируют комбинации соперника.
*   *Сейчас:* Бот играет только от своей силы.
*   *Нужно:* Блефовать чаще, если наши карты мешают сопернику иметь "натс".

### 4. Multithreading (Rayon)
Сейчас код работает в одном потоке. Использование крейта `rayon` позволит распараллелить итерации CFR, ускорив обучение в `N` раз (где N — ядра CPU).

---
*License: MIT*